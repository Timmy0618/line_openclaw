[project]
name = "line-bridge"
version = "0.1.0"
description = "LINE webhook bridge that forwards messages to OpenClaw's OpenAI-compatible API"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "httpx>=0.27",
    "uvicorn[standard]>=0.30",
]

[tool.uv]
package = false
